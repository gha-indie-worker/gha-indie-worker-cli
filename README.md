# gha-indie-worker-cli

Rust CLI for Independent GitHub Actions worker and clone-server control plane. Uses flags-2-env via `.cli-flags.toml`. Talks to `gha-indie-worker-api-server.rs`.
Env keys are generated by `f2e generate` from `.cli-flags.toml` into `generated/{rust,dart,typescript,gleam}`.
